# Dependencies
node_modules/

# Build output
dist/

# Environment — never commit API keys
.env

# Runtime data — NEVER commit. Contains WhatsApp/Slack messages,
# session tokens, conversation logs, and database files.
store/
workspace/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Migration state — local only
migrations/.applied.json

# Logs
*.log
/tmp/claudeclaw.*

# Claude Code local settings (user-specific permissions, not for repo)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
.wwebjs_cache/

# Personal scripts — not for sharing
scripts/waste-reminder.sh

# Personal config — lives in CLAUDECLAW_CONFIG (default ~/.claudeclaw)
CLAUDE.md
agents/*/agent.yaml
agents/*/CLAUDE.md
logs/
data/poly-probe/

# Transient news-sync output (auto-pruned, see HEARTBEAT.md)
docs/news/

# Sprint 4 research ingestion run summaries — content is also in SQLite
docs/research/ingestions/

# External skill scratch clones (nested .git dirs, unrelated to trading)
caveman-skill-tmp/
